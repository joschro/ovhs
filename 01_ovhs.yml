---

- name: setup oVirt host
  hosts: localhost
  connection: local
  become: yes
  gather_facts: no
    
  roles:
#    - { role: ovhs-base-config, tags: ["ovhs-base-config"] }
    - { role: ovhs-gluster-master, tags: ["ovhs-gluster-master"] }
#    - { role: ovhs-ovirt-install, tags: ["ovhs-ovirt-install"] }

  vars_files:
    - vars/network.yml
