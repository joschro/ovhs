---

- name: Install oVirt Hosted Engine
  hosts: ovhs
  connection: local
    
  vars:
    ovirt_repositories_ovirt_release_rpm: "http://plain.resources.ovirt.org/pub/yum-repo/ovirt-release43.rpm"
#  vars_files:
#    - vars/network.yml
  roles:
#    - displayfacts 
    - { role: ovhs.hostname, tags: ["networking"] }
    - { role: ovhs.etc_hosts, tags: ["networking"] }
    - { role: ovhs.network, tags: ["networking"] }
    - { role: ovhs.dnsmaq, tags: ["networking"] }
    - { role: ovhs.gluster, tags: ["storage"] }
    - { role: ovirt.repositories, tags: ["ovirt"] }
    - { role: ovirt.hosted_engine_setup, tags: ["ovirt"] }
